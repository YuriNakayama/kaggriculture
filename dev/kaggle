#!/bin/bash
# Kaggle competition CLI (submissions, episodes, leaderboard).
#
#   dev/kaggle submissions
#   dev/kaggle episodes <SUBMISSION_ID>
#   dev/kaggle leaderboard kaggriculture -s
set -euo pipefail
cd "$(dirname "$0")/.."
exec uv run --directory backend kaggle competitions "$@"
