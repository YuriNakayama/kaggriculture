#!/bin/bash
# Run the pytest suite. Extra args pass through:
#   dev/test                       # everything
#   dev/test tests/unit -x         # a subset
#   dev/test -m "not e2e"          # CI's fast slice
set -euo pipefail
cd "$(dirname "$0")/.."
exec uv run --directory backend pytest "$@"
